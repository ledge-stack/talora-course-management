const { SignJWT } = require('jose'); async function test() { const secret = new TextEncoder().encode('talora-super-secret-development-key-change-me'); const token = await new SignJWT({ userId: 'test-user', roles: [] }).setProtectedHeader({ alg: 'HS256' }).setIssuedAt().setExpirationTime('24h').sign(secret); const res = await fetch('https://talora-course-management-web.vercel.app/api/v1/users/test-user', { method: 'PATCH', headers: { 'Content-Type': 'application/json', 'Cookie': \	alora_token=\\ }, body: JSON.stringify({ phoneNumber: '+256771234567' }) }); const text = await res.text(); console.log('Status:', res.status); console.log('Response:', text); } test();
